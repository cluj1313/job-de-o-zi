/** Warm collage cover assembled from parts (MCP-friendly). */
import { p0 } from './coverJobsPart0'
import { p1 } from './coverJobsPart1'
import { p2 } from './coverJobsPart2'
import { p3 } from './coverJobsPart3'
export const COVER_JOBS_DATA_URL = 'data:image/jpeg;base64,' + p0 + p1 + p2 + p3
export const coverJobs = COVER_JOBS_DATA_URL
